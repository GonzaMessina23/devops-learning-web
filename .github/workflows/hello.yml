name: Mi primer CI

on:
  push:
    branches:
      - main

jobs:
  test:
      runs-on: ubuntu-latest

      steps:
        - name: Checkout
          uses: actions/checkout@v4

        - name: Hola
          run: echo "Estoy aprendiendo GitHub Actions"
    
        - name: Segundo step
          run: echo "Este es mi segundo step"

